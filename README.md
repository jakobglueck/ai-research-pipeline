# From Prompt to Pipeline: Autonomous AI Research with Claude Code

A controlled empirical study comparing two conditions for autonomous scientific
paper writing using Claude Code (Sonnet 4.6) on the Stack Overflow Developer
Survey 2024 (N = 65,437).

## What this project is

**Condition A — Baseline:** A single prompt instructs the agent to find a CSV,
devise a research question, analyse the data, and produce a six-page LaTeX paper.
No tools beyond the local filesystem.

**Condition B — Pipeline:** A five-stage instruction chain with two MCP tools:
- `sqlite-mcp` — structured SQL access to the survey database
- `fetch-mcp` — live web retrieval for literature and citation verification

Both conditions use the same model and dataset with **zero human interventions
per run**. The pipeline was run four times; each run exposed one failure mode
that required prompt engineering to resolve.

The final pipeline paper — *Fear Over Pay* — finds that perceived AI job threat
is a stronger predictor of developer job satisfaction than annual compensation
(Cohen's d = 0.36, 29/29 QC checks passed, 0 hallucinated citations).

A meta-report documenting the full engineering journey is included in
`metareport/`.

## Repository structure

```
stages/                  Pipeline stage instruction files (Stage 0–4)
run_experiment.md        Single-trigger file — runs all five stages at once
prompts/
  baseline_prompt.md     The single-prompt baseline instruction
scripts/                 Python scripts generated by the pipeline
  setup_db.py            CSV → SQLite import
  mcp_helpers.py         Helper functions (correlations, group stats)
  preprocessing.py       Variable encoding with justification log
  analysis.py            OLS regression, VIF, effect sizes
  generate_figures.py    All three figures
  qc_check.py            Automated QC with regex verification
baseline/
  baseline_output.tex    The baseline paper (LaTeX source)
experiment/
  experiment_output.tex  The pipeline paper (LaTeX source)
  figures/               Three PNG figures referenced in the paper
logs/
  stage_0_log.md         Setup and database import log
  stage_1_log.md         Exploration, research question, 7 verified sources
  stage_2_log.md         Statistical analysis with practical significance
  stage_3_log.md         Paper writing decisions
  stage_4_log.md         QC stdout (29/29 PASS) and compilation log
  baseline_log.md        Baseline session log
  baseline_auswertung.md Baseline evaluation
  metareport_content.md  Meta-report planning notes
  metareport_related_work.md Related work with verified abstracts
metareport/
  metareport.tex         Meta-report LaTeX source
  bibliography.bib       All verified BibTeX entries
  splncs03.bst           Bibliography style
data/
  survey_results_schema.csv  Column schema (public metadata)
  survey_results_public.csv  NOT INCLUDED — download separately (see below)
```

## Dataset

The Stack Overflow Developer Survey 2024 is publicly available at:
[https://survey.stackoverflow.co/2024/](https://survey.stackoverflow.co/2024/)

Download `survey_results_public.csv` and place it in `data/` before running
the pipeline.

## Running the pipeline

### Requirements

- [Claude Code](https://claude.ai/code) with Sonnet 4.6
- [uvx](https://docs.astral.sh/uv/) for MCP servers
- Python 3.10+ with `pandas`, `scipy`, `statsmodels`, `matplotlib`, `seaborn`
- [tectonic](https://tectonic-typesetting.github.io/) for LaTeX compilation

### MCP configuration

Create `.claude/settings.json` in this directory:

```json
{
  "mcpServers": {
    "sqlite": {
      "command": "uvx",
      "args": ["mcp-server-sqlite", "--db-path", "./db/survey.db"]
    },
    "fetch": {
      "command": "uvx",
      "args": ["mcp-server-fetch"]
    }
  },
  "permissions": {
    "allow": ["Bash(*)", "Read(*)", "Write(*)", "Edit(*)", "mcp__sqlite__*", "mcp__fetch__*"]
  }
}
```

### Trigger

Open Claude Code in this directory and send:

```
Read and execute completely: run_experiment.md
```

The pipeline runs all five stages autonomously without further user input.

## Key findings from the engineering journey

| Run | Failure mode | Fix applied |
|-----|-------------|-------------|
| 1 | Privacy: real email injected from system context | Anonymous Author constraint |
| 2 | QC declared PASS without executing code | STDOUT proof required |
| 2 | Citations without reading abstracts | Abstract required via Fetch-MCP |
| 3 | Effect sizes trivially small (f²=0.00033) | Effect-Size-Gate added |
| 3 | Same research question as Run 2 | Interaction gate tightened (f²≥0.02) |
| 3 | Dummy-coding rule overridden (**Reasoned Non-Compliance**) | Code assertions over text rules |
| 4 | No new failure mode — 29/29 QC, d=0.36, genuine finding | — |

## Meta-report

`metareport/metareport.tex` is a peer-review-style paper documenting the full
engineering journey. Compile with:

```bash
cd metareport && tectonic metareport.tex
```

## License

MIT — code and pipeline instructions are free to reuse.
The survey data belongs to Stack Overflow and is not included in this repository.
Download it directly from [survey.stackoverflow.co/2024](https://survey.stackoverflow.co/2024/).
